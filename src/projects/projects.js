const github = '';

function render(project) {
  // 1. Get the projects div using getElementById() call it projects;
  // 6. Create a div called projectDiv
  // 7. Create a p element called projectName
  // 7.1. Set the innerHTML to project.name
  // 8. Append the projectName to the projectDiv
  // 9. Append the projectDiv to the projects
}

async function onPageLoad() {
  // 2. Fetch the projects off of GitHub using
  // fetch(`https://api.github.com/users/${github}/repos`)
  // 3. Turn the response into data by doing
  // let projects = await response.json();
  // 4. Iterate the projects variable using a for...of loop
  // 5. Call the render function that will add it to the page
}
